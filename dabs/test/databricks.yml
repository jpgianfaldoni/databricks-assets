# This is a Databricks asset bundle definition for test.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: test
  uuid: 535b6817-05e7-40d6-8417-565dc6391132

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-194dc537-bb28.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-194dc537-bb28.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/9d58aae1-d4c7-403a-a8cc-7dee1a95f901 to make sure we only have a single copy.
      root_path: /Workspace/Users/9d58aae1-d4c7-403a-a8cc-7dee1a95f901/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - service_principal_name: 9d58aae1-d4c7-403a-a8cc-7dee1a95f901
        level: CAN_MANAGE
